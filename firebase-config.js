// Firebase is opt-in so the game remains playable without a hosted project.
// Paste the web app config from Firebase Console here, then set enabled to true.
window.FLOOR13_FIREBASE_CONFIG = {
  enabled: false,
  config: {
    apiKey: "",
    authDomain: "",
    projectId: "",
    appId: ""
  }
};
